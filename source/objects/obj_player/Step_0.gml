event_inherited()

timers()

final_hsp = hsp*spd
final_vsp = vsp

updateCollision()
 
if keyboard_check_pressed(vk_control) && keyboard_check(ord("D")) && DEBUG {
	room_goto(rm_debug);	
}

if array_length(ovos) > 0 {
	//ele vai seguir eu to com preguica faz por mim purfavorzinhoo
}

if y > room_height+100 {morte()}